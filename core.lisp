(defun get-parameters (fn)
  ())

(defun get-body (fn)
  ())

; function 
(defun write-function (fn &key (lang 'java))
  (write-parameters fn lan)
  (write-body fn lan))

(defun write-parameters (params lang)
  (let ((pen (get-global-cursor)))
    '()))

(defun write-body (bdy lang)
  (let ((pen (get-global-cursor)))
    '()))

